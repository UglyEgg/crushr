# TASK_PACKET

Task ID
CRUSHR-FORMAT-05-f3

Title
Make FORMAT-05 comparison runnable by aligning crushr-lab-salvage with the actual crushr-pack experimental writer surface

Role
BUILDER

Objective
Fix the exact end-to-end failure preventing FORMAT-05 result generation.

Observed human-run failure:

    cargo run -p crushr --bin crushr-lab-salvage -- \
      run-format05-comparison \
      --output /tmp/crushr_format05_cmp

fails with:

    crushr-pack failed
    unsupported flag: --experimental-self-identifying-blocks

This packet is not about new semantics.
It is only about making the existing FORMAT-05 workflow operational.

---------------------------------------------------------------------

Read first

1. AGENTS.md
2. AI_BOOTSTRAP.md
3. REPO_GUARDRAILS.md
4. PROJECT_STATE.md
5. .ai/INDEX.md
6. .ai/STATUS.md
7. .ai/PHASE_PLAN.md
8. .ai/DECISION_LOG.md
9. .ai/HANDOFF.md
10. crates/crushr/src/bin/crushr-pack.rs
11. crates/crushr/src/bin/crushr-lab-salvage.rs
12. all recent FORMAT-05 implementation and fix packets

---------------------------------------------------------------------

Context

The active `crushr-lab-salvage` FORMAT-05 comparison runner invokes an experimental `crushr-pack` flag that the actual binary does not support.

The human has confirmed this exact failure repeatedly.

This packet must make the workflow run end-to-end and produce real FORMAT-05 comparison output files.

---------------------------------------------------------------------

Hard constraints

- Do NOT change FORMAT-05 semantics.
- Do NOT redesign the comparison workflow.
- Do NOT add more indirection or help-probing.
- Do NOT leave the runner calling nonexistent writer flags.
- Keep the fix tightly scoped.

---------------------------------------------------------------------

Required implementation scope

1) Inspect and define the actual canonical experimental writer contract for FORMAT-05

Choose one truthful contract and implement it consistently:

A) `crushr-pack` supports `--experimental-self-identifying-blocks`
OR
B) `crushr-pack` supports a different canonical flag name and `run-format05-comparison` must use that name
OR
C) both names are supported, with one treated as an alias

Pick one and make code/tests/docs agree.

2) Align the runner

`run-format05-comparison` must invoke the actual supported writer contract.
No stale flag names.
No probing via `--help`.

3) Add direct regression test

Add one test that proves the exact human command works end-to-end:

    run-format05-comparison --output <dir>

and that it produces:
- format05_comparison_summary.json
- format05_comparison_summary.md

4) Optional but recommended

Add bounded `crushr-pack --help` support if easy, but this is not the main requirement.
Do not make the runner depend on it.

---------------------------------------------------------------------

Acceptance criteria

This packet is complete only if:

1) `run-format05-comparison --output <dir>` runs successfully
2) the runner no longer invokes a nonexistent packer flag
3) FORMAT-05 comparison summary files are produced
4) regression test covers the exact failure mode
5) workspace gates pass

---------------------------------------------------------------------

Verification commands

cargo fmt --all --check

cargo test --workspace

cargo clippy --workspace --all-targets -- -D warnings

Additionally verify explicitly:

cargo run -p crushr --bin crushr-lab-salvage -- \
  run-format05-comparison \
  --output "$(mktemp -d)"

---------------------------------------------------------------------

Required response format

Return:

1) what changed
2) files modified
3) verification commands and outcomes
4) remaining risks
5) exact canonical FORMAT-05 writer flag now supported
6) exact working comparison command
7) completion status
